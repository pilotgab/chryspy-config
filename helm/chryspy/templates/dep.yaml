apiVersion: apps/v1
kind: Deployment
metadata:
  name: chryspyapp
  labels: 
    app: chryspyapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chryspyapp
  template:
    metadata:
      labels:
        app: chryspyapp
        github-sha: "${{ GITHUB_SHA }}"
    spec:
      containers:
      - name: chryspyapp
        image: captaingab/chrispyimg:latest
        ports:
        - name: chryspyimg-port
          containerPort: 80
      
       
