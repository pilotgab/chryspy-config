apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chryspy.fullname" . }}-test-connection"
  labels:
    {{- include "chryspy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chryspy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
